# v1.1 — capability rewrite, clear-state, fail-open split, URI rules, installer hardening

status: DONE
outcome: quietlsp v1.1 per docs/SPEC.md R1..R12, installed and live-verified on this workstation.
source request: dispatch prompt 2026-08-15, "implement QuietLSP v1.1 per amended spec".

## acceptance criteria (from SPEC.md)
1. `./install-quietlsp --status` clean (exit 0, all wrapped).
2. Integration test (R10) green and saved in tests/ as a rerunnable artifact.
3. Capability rewrite verified in a recorded initialize exchange.
4. Cross-worktree noise absence — owner-observed checklist item, not producible here.

## constraints
- Never hand-edit tracked plugin cache files.
- No pull-mode filtering (roadmap, owner-gated).
- Message rewriting permitted ONLY for the R1 capability strip.
- rust-analyzer binary absent on this box (only a non-functional rustup stub) —
  R10 rust-analyzer case is structural-only, named as a gap, not faked green.
- Do not kill/restart any running language-server process.

## execution steps (commit order)
1. R1 capability rewrite: parse client `initialize`, strip `capabilities.textDocument.diagnostic`,
   recompute Content-Length for that frame only, log original+rewritten once.
2. R6 clear-state: per-URI forwarded-set; pass through an already-empty out-of-scope
   publication when the URI was previously forwarded (never synthesize); version untouched.
3. R7 split fail-open + R11 shutdown: header/Content-Length fault = permanent passthrough;
   per-frame JSON/classify fault = pass that frame, keep filtering; ordered async-safe shutdown,
   child-stdin close on client EOF, drain, exit 128+n on signal death, 5s deadline.
4. R4 URI rules: `file:` scheme only via `new URL()`, deepest-existing-ancestor canonicalization,
   component-based prefix containment.
5. R3/R5: log rootUri/workspaceFolders vs cwd disagreement; workspaceFolders (validated dirs)
   as additional allowed roots.
6. R8/R9 installer: pre-validate all targets before any write, atomic per-target install,
   recorded hashes/metadata under ~/.local/state/quietlsp/, refuse collisions, full-tuple
   --status (wrapped/drifted/unwrapped) incl. launch probe, --uninstall removes only recorded.
7. R10/R12 integration test against installed typescript-language-server, saved in tests/;
   rust-analyzer gap named (no functional binary on this box).

## current receipt
All 7 steps landed, one commit each, tests green throughout:
1. a6e651e R1 capability rewrite
2. 0ff4bc2 R6 clear-state per-URI forwarded-set
3. 742c883 R7/R11 fail-open split + ordered shutdown
4. 20b1d4e R4 file: URI classification + deepest-existing-ancestor canonicalization
5. 28a0786 R3/R5 root evidence + workspaceFolders extra roots
6. 54fac1a R8/R9 installer pre-validation, nested-wrapper refusal, recorded installs, launch-probe status
7. c9172c4 R10/R12 real integration test vs installed typescript-language-server

Final state: 26 filter tests + 10 installer tests + 5 integration tests (all
green), `./install-quietlsp --status` exits 0 (`wrapped=2 stale=0 unwrapped=0
probe_failed=0`), live handshake through the installed shim path returns
capabilities and the capability-rewrite log line is recorded.

Named gaps (see acceptance item 4 and R10 rust-analyzer clause):
- rust-analyzer: no functional binary on this box (only a non-functional
  `rustup` proxy stub) — integration test reports this as a GAP line, does
  not fake a pass.
- Cross-worktree noise absence in an interactive session is owner-observed
  per spec, not producible by a dispatched session.
- typescript-language-server 5.9.3 never advertises `diagnosticProvider`
  regardless of the capability rewrite, so the R1 proof is the wrapper's own
  log line (original vs rewritten `textDocument`), not a negotiated-capability
  diff — documented as an explicit "honest gap" test, not dressed up.

## next executable action
None — v1.1 shipped and pushed to origin/main. Re-run `./install-quietlsp`
after any future plugin version bump (creates a fresh, unwrapped bin/ dir).
